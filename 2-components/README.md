# components 

Component React ka ek independent, reusable block hota hai jo UI ka ek specific part represent karta hai.
Har component apna structure, logic, aur data manage kar sakta hai.

A component in React is a reusable and independent piece of UI that returns JSX and controls how a part of the interface looks and behaves.